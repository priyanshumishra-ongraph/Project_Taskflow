// Production environment (used by `ng build` via angular.json fileReplacements).
// Update apiUrl if your deployed Render backend URL changes.
export const environment = {
  production: true,
  apiUrl: 'https://worksy-ibum.onrender.com/api'
};
